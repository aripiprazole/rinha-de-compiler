module Main

external function println(message)

function fib(n) {
  if n < 2 {
    return n;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

function main() {
  println("fib " ++ fib(1000));
  println("hello, world");
}
