external function println(message);
function fib(n) {
  if (n < 2) {
    n
  } else {
    fib(n - 1) + fib(n - 2)
  }
};

let x = 10;
let n = f ++ f2;
let _ = println(x);
let _ = println(n);
let _ = println("fib " ++ fib(100000));
let _ = println(f());
let _ = println(f2("pao"));
let _ = println((10 * (14 + 3)));
println("hello, world")
